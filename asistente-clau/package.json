{
  "name": "asistente-clau",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Asistente de correo por voz para operaciones entre Indonesia y Paraguay",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "demo": "DEMO=1 node --env-file-if-exists=.env src/server.js",
    "sync": "node --env-file-if-exists=.env src/sync.js --once"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@azure/msal-node": "^6.0.0",
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^10.1.3",
    "fastify": "^5.2.1",
    "zod": "^4.4.3"
  }
}
